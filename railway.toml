[build]
# Define the build environment packages
packages = [
    "node@20", 
    "python@3.11", 
    "ffmpeg", 
    "yt-dlp" 
]

# Explicitly install python dependencies first, then node dependencies
installCommand = "pip3 install yt-dlp && npm ci"

[deploy]
startCommand = "node ."