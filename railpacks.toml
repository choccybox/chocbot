[build]
# This ensures python, ffmpeg, and yt-dlp are available during 'npm ci'
packages = [
  "nodejs-20_x",
  "python3",
  "ffmpeg",
  "yt-dlp"
]

[deploy]
# Using 'node .' as requested in your original nixpacks config
startCommand = "node ."