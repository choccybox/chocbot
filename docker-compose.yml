services:
  chocbot:
    build: .
    container_name: chocbot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 3000
    ports:
      - "3000:3000"
    volumes:
      - chocbot-temp:/app/temp
    init: true

volumes:
  chocbot-temp:
